{
  /* See /tsconfig.json for actual values */
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      /* Configure path aliases for non-terminal monorepo packages */
      "@architus/lib/*": ["../lib/src/*"]
    }
  }
}
